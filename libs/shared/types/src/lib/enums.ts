export enum EPizzaSize {
  small = 'sm',
  medium = 'md',
  large = 'lg',
}

export enum EUserRole {
  admin = 'admin',
  client = 'client',
}

export enum EReportFormat {
  pdf = 'pdf',
  html = 'html',
}

export enum EThemes {
  dark = 'minima-dark',
  light = 'minima-light',
}
